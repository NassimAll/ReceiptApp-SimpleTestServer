export interface User {
    id: number;
    username: string;
    name: string;
    surname: string;
    password: string;
  }

  export interface TokenUser {
    id: number;
    username: string;
    
  }
